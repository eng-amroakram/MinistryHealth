<input
    type="{{ $type }}"
    form="{{ $form }}"
    dir="{{ $dir }}"
    class="{{ $class }}"
    name="{{ $name }}"
    id="{{ $id }}"
    revers="{{ $revers ?? '' }}"
    reversone="{{ $reversone ?? '' }}"
    reverstwo="{{ $reverstwo ?? '' }}"
    reversthree="{{$reversthree ?? ""}}"
    placeholder="{{ $placeholder }}" />
